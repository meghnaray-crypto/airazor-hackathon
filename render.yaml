services:
  - type: web
    name: airazor-hackathon
    runtime: python
    plan: free
    buildCommand: "echo 'No build step required'"
    startCommand: "python3 server.py"
    healthCheckPath: /health
